# In production, frontend is served by Express on the same origin.
# Empty string makes all API calls use relative paths (/api/...).
VITE_API_BASE_URL=
# VITE_API_KEY must be set as a Railway environment variable (baked in at build time).
